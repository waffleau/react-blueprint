Boilerplate for React projects, including redux, router, and storybook. Provides some common components and structures.

# Usage

Run the app: `yarn start`

Run storybook: `yarn run storybook`

Prod build: `yarn run build`
